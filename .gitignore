node_modules/
dist/

# DynamoDB Local: the vendored runtime (~57MB — a 5MB jar plus its native libs)
# and its on-disk database. Re-fetch with `npm run ddb:install`.
vendor/
.dynamodb-data/

# Local environment overrides. `.env.example` IS tracked.
.env
.env.local

.DS_Store
*.log
__pycache__/

# Graph exports, in either format. `npm run graph:export` writes one; it is your data, not
# the repo's. A text graph worth keeping goes in under a name you chose.
graph-export*.json
graph-export*.txt

# Screenshots from `node scripts/drive-map.mjs`. Evidence for one change, not the repo's.
.shots/
